export const publicRoutes = [
  "/",
];

export const authRoutes = [
  "/auth/login",
  "/auth/error",
];

export const identityRoutePrefix = "/identity" 

export const apiRoutesPrefix = "/api"

export const DEFAULT_LOGIN_REDIRECT = "/dashboard";